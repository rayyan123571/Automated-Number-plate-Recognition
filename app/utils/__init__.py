# =============================================================================
# app/utils/__init__.py
# =============================================================================
# PURPOSE:
#   Package marker for the `utils` module.  Utilities are pure helper
#   functions with no side effects — image decoding, format conversion,
#   validation helpers, etc.
#
# WHY IT EXISTS:
#   Keeps reusable, stateless helper logic separate from business logic
#   and framework code.  Easy to unit-test in isolation.
# =============================================================================
